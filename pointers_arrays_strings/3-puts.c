#include "main.h"
/**
 *  *  * _strlen - Writes the length of a string.
 *   *   * @str: The length of the string.
 *    **/
void_putss(char *str)
{
	while(*str)
	_putchar(*str++);
	_puthcar('\n');
}
