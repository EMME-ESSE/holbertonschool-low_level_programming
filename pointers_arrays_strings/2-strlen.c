#include "main.h"
/**
 *  * _strlen - Writes the length of a string.
 *   * @s: The length of the string .
 *    * Return: 0 
 *     */


int _strlen(char *s)
{
	int len;
	char s[50];
	len = _strlen();
	return(0);
}
