#include "main.h"
/**
 *   * mul - Multiplaying function for two integers
 *   * @a: first integer.
 *   * @b: second integer to multiply
 *    *Return: a+b.
**/
int mul(int a, int b)
{
return (a * b);
}
