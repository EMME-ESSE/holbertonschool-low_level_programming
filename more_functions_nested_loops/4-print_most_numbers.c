#include "main.h"
/**
 *  * print_numbers - Prints the numbers from 0-9.
**/
void print_most_numbers(void)
{
int c;	
		if (c >= '0' && c <= '9')
			if (c != 4 || c != 2)
				return (1);
		else
			return (0);

}
