#include <stdlib.h>
#include <time.h>
/**
*** main - Entry point
*** Return: Always 0 (Success)
**/
int main(void)
{
int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
digito = n % 10
if (digito > 5)
printf("Last digif of %n is greater than 5")
else if (digito == 0)
printf("Las digit of %n is 0")
else if (digito < 6 && digit != 0)
printf("Las digit of %n is less than 6 and not 0")
return (0);
}
