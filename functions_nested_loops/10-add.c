#include "main.h"
/**
 *  * add - Add two input integers and makes the result between them
 *   *@a,b - both the integer input numbers
 *    * Return: The result of the nums..
 *     */
int add(int a, int b)
	{
	return (a + b);
	}
