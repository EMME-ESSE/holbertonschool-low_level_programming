#include "main.h"
/**
** _abs functionn - Check Holberton
** @n: im done
** Description: _abs function prints the sign of the number
** Return: positive
**/
int _abs(int)
{
	int r;
		if (r >= 0)
					return (r);
			else
						return (-r);
}
